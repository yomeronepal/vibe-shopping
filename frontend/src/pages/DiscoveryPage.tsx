import React from 'react';
import OutfitFeed from '../components/discovery/OutfitFeed';

const DiscoveryPage: React.FC = () => {
    return (
        <div className="bg-black min-h-screen">
            <OutfitFeed />
        </div>
    );
};

export default DiscoveryPage;
